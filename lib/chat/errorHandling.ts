import { Context } from './types';export function handleHydrationError(e: any, context: any, sheetName: string) {    const msg = String(e?.message || e || 'Unknown error');    const errorMsg = `Failed to load '${sheetName}': ${msg}`;    if (!Array.isArray(context.errors)) context.errors = [];    context.errors.push(errorMsg);    context.error = errorMsg;    if (msg.includes('400')) context.error += ' (invalid sheet configuration)';    else if (msg.includes('403')) context.error += ' (check service account permissions)';    else if (msg.includes('404')) context.error += ' (tab not found)';}export function handleGenericHydrationError(e: any, context: any) {    const name = String((context && context.sheetName) || '');    const msg = String(e?.message || e || 'Unknown error');    context.error = `Failed to load '${name}': ${msg}`;    if (msg.includes('400')) context.error += ' (invalid sheet configuration)';    else if (msg.includes('403')) context.error += ' (check service account permissions)';    else if (msg.includes('404')) context.error += ' (tab not found)';    context.sheetData = context.sheetData || {};    context.quickReplies = [        { text: `Check tab: ${name}`, action: 'clarify_sheet' },        { text: 'Retry', action: 'retry_hydration' }    ];}export function createUserFriendlyError(context: any): boolean {    try {        if (typeof context?.error === 'string' && context.error.trim()) {            const err = context.error as string;            const name = String(context.sheetName || '').trim();            let msg = '';            if (err.includes('404')) {                msg = `Sheet '${name}' not found. Did you mean another name?`;            } else if (err.includes('403')) {                msg = 'Permission issue with sheet access. Please check your credentials.';            } else {                msg = `Couldn\'t load '${name}': ${err}. Try another sheet or upload data.`;            }            context._specificErrorResponse = msg;            try {                context._uiActions = context._uiActions || [];                context._uiActions.push(                    { text: 'Specify sheet name', action: 'clarify_sheet' },                    { text: 'Upload file', action: 'upload' }                );            } catch {}            return true;        }    } catch {}    return false;}export function handleFatalError(e: any, context: Context) {    // eslint-disable-next-line no-console    console.error('[processMessage] FATAL', e);    return {      response: `An unexpected error occurred: ${e.message}`,      toolCalls: [],      pendingToolCalls: [],      toolResults: [],      context,      quickReplies: ['Start over']    };}