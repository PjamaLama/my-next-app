rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow temporary uploads with auto-deletion
    match /temp-uploads/{fileName} {
      allow read, write: if request.auth != null;
      // Files in temp-uploads can be read/written by authenticated users
    }
  }
}
